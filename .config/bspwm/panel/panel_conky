conky.config = {
    short_units  = true,
    background = true,
    out_to_console = true,
    out_to_x = false,
    update_interval = 1,
    update_interval_on_battery = 5,
};

conky.text =[[
S${execi 1 ./panel_playerctl} ${execi 1 ./panel_tray_dummy} ${execi 1 ./panel_temperature} ${execi 1 ./panel_wifi} ${execi 1 ./panel_battery} ${time %H:%M} %{-u}
]];
